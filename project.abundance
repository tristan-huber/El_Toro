{
  "atomType": "Molecule",
  "x": 0,
  "y": 0,
  "uniqueID": "id-996",
  "name": "El_Toro",
  "topLevel": true,
  "allAtoms": [
    {
      "atomType": "Output",
      "x": 0.98,
      "y": 0.5,
      "uniqueID": "id-997",
      "ioValues": [
        {
          "name": "number or geometry",
          "ioValue": "__GEOMETRY_INPUT__"
        }
      ]
    },
    {
      "atomType": "Molecule",
      "x": 0.26171875,
      "y": 0.40571428571428575,
      "uniqueID": "id-999",
      "name": "Molecule",
      "ioValues": [
        {
          "name": "Input",
          "ioValue": [
            "2",
            "4",
            "5",
            "6"
          ]
        }
      ],
      "topLevel": false,
      "allAtoms": [
        {
          "atomType": "Output",
          "x": 0.98,
          "y": 0.5,
          "uniqueID": "id-1001",
          "ioValues": [
            {
              "name": "number or geometry",
              "ioValue": "__GEOMETRY_INPUT__"
            }
          ]
        },
        {
          "atomType": "Code",
          "x": 0.24023437499999997,
          "y": 0.4628571428571429,
          "uniqueID": "id-1003",
          "interpreterVersion": 1,
          "code": "\n/**\n * This code atom implements a simple linear layout funtion which repeats\n * shape count times in the positive X direction with offset space\n * between each. Works for either 2D or 3D shapes. Negative offset\n * and negative counts invert the direction.\n * \n * Notice that defaults are set for both count and offset.\n */\n\nconst pointsAndSpline = (points: [number, number, number][]) => {\n  console.log('bah')\n  const result = [];\n  result.push(\n    replicad.makeBSplineApproximation(points, {\n      degMax: 3,\n      degMin: 1,\n      tolerance: 0.01,\n    })\n  );\n\n  const q = replicad.makeSphere(0.5);\n  for (const p of points) {\n    console.log(p);\n    result.push(q.clone().translate(p[0], p[1], p[2]));\n  }\n  return result;\n};\n\nfunction pointsWithCamberedStart(\n  points: [number, number][],\n  stations: number[],\n  frontCamber: number,\n  backCamber: number\n): [number, number, number][] {\n  const result = [];\n  for (let i = 0; i < points.length; i++) {\n    let x = stations[i];\n    let z = points[i][1];\n    if (i == 0) {\n      x = Math.cos(frontCamber) * z; // instead of 0\n      z = Math.sin(frontCamber) * z;\n    }\n    if (i == points.length - 1) {\n      x = x - Math.cos(backCamber) * z;\n      z = Math.sin(backCamber) * z;\n    }\n\n    result.push([x, points[i][0], z]);\n  }\n  //@ts-ignore\n  return result;\n};\n\nfunction run() {\n  const keel_line: [number, number][] = [\n    [0, 16 + 7 / 16],\n    [0, 18 + 7 / 8],\n    [0, 21 + 7 / 8],\n    [0, 21],\n    [0, 18 + 9 / 16],\n  ];\n\n  const sheer: [number, number][] = [\n    [10 + 3 / 8, 2 + 11 / 16],\n    [18 + 9 / 16, 4 + 11 / 16],\n    [22.25, 6 + 3 / 16],\n    [22, 6 + 7 / 16],\n    [19 + 7 / 16, 5 + 11 / 16],\n  ];\n\n  const chine: [number, number][] = [\n    [9 + 1 / 16, 12.25],\n    [15.25, 15 + 1 / 8],\n    [19.25, 18 + 11 / 16],\n    [19 + 9 / 16, 17 + 13 / 16],\n    [17 + 5 / 16, 14.5],\n  ];\n\n  const stationDists = [23 + 1 / 8, 24, 24, 23 + 1 / 8];\n\n  const spoints = [0];\n  for (const offset of stationDists) {\n    spoints.push(spoints[spoints.length - 1] + offset);\n  }\n  const frontCamber = 61 * Math.PI / 180\n  const rearCamber = 80 * Math.PI / 180\n\n  const t = pointsWithCamberedStart(keel_line, spoints, frontCamber, rearCamber);\n\n\n\n  const blep = [\n    pointsAndSpline(pointsWithCamberedStart(keel_line, spoints, frontCamber, rearCamber)),\n    pointsAndSpline(pointsWithCamberedStart(chine, spoints, frontCamber, rearCamber)),\n    pointsAndSpline(pointsWithCamberedStart(sheer, spoints, frontCamber, rearCamber)),\n  ];\n\n  console.log(\"blep\")\n  console.log(blep)\n  console.log(blep.length)\n  const wires = []\n  for (const res of blep) {\n    //@ts-ignore\n    wires.push(replicad.assembleWire([res[0]]))\n  }\n  console.log(wires)\n  return replicad.loft(wires)\n}",
          "compiledCode": "\"use strict\";\n/**\n * This code atom implements a simple linear layout funtion which repeats\n * shape count times in the positive X direction with offset space\n * between each. Works for either 2D or 3D shapes. Negative offset\n * and negative counts invert the direction.\n *\n * Notice that defaults are set for both count and offset.\n */\nconst pointsAndSpline = (points) => {\n    console.log('bah');\n    const result = [];\n    result.push(replicad.makeBSplineApproximation(points, {\n        degMax: 3,\n        degMin: 1,\n        tolerance: 0.01,\n    }));\n    const q = replicad.makeSphere(0.5);\n    for (const p of points) {\n        console.log(p);\n        result.push(q.clone().translate(p[0], p[1], p[2]));\n    }\n    return result;\n};\nfunction pointsWithCamberedStart(points, stations, frontCamber, backCamber) {\n    const result = [];\n    for (let i = 0; i < points.length; i++) {\n        let x = stations[i];\n        let z = points[i][1];\n        if (i == 0) {\n            x = Math.cos(frontCamber) * z; // instead of 0\n            z = Math.sin(frontCamber) * z;\n        }\n        if (i == points.length - 1) {\n            x = x - Math.cos(backCamber) * z;\n            z = Math.sin(backCamber) * z;\n        }\n        result.push([x, points[i][0], z]);\n    }\n    //@ts-ignore\n    return result;\n}\n;\nfunction run() {\n    const keel_line = [\n        [0, 16 + 7 / 16],\n        [0, 18 + 7 / 8],\n        [0, 21 + 7 / 8],\n        [0, 21],\n        [0, 18 + 9 / 16],\n    ];\n    const sheer = [\n        [10 + 3 / 8, 2 + 11 / 16],\n        [18 + 9 / 16, 4 + 11 / 16],\n        [22.25, 6 + 3 / 16],\n        [22, 6 + 7 / 16],\n        [19 + 7 / 16, 5 + 11 / 16],\n    ];\n    const chine = [\n        [9 + 1 / 16, 12.25],\n        [15.25, 15 + 1 / 8],\n        [19.25, 18 + 11 / 16],\n        [19 + 9 / 16, 17 + 13 / 16],\n        [17 + 5 / 16, 14.5],\n    ];\n    const stationDists = [23 + 1 / 8, 24, 24, 23 + 1 / 8];\n    const spoints = [0];\n    for (const offset of stationDists) {\n        spoints.push(spoints[spoints.length - 1] + offset);\n    }\n    const frontCamber = 61 * Math.PI / 180;\n    const rearCamber = 80 * Math.PI / 180;\n    const t = pointsWithCamberedStart(keel_line, spoints, frontCamber, rearCamber);\n    const blep = [\n        pointsAndSpline(pointsWithCamberedStart(keel_line, spoints, frontCamber, rearCamber)),\n        pointsAndSpline(pointsWithCamberedStart(chine, spoints, frontCamber, rearCamber)),\n        pointsAndSpline(pointsWithCamberedStart(sheer, spoints, frontCamber, rearCamber)),\n    ];\n    console.log(\"blep\");\n    console.log(blep);\n    console.log(blep.length);\n    const wires = [];\n    for (const res of blep) {\n        //@ts-ignore\n        wires.push(replicad.assembleWire([res[0]]));\n    }\n    console.log(wires);\n    return replicad.loft(wires);\n}\n"
        },
        {
          "atomType": "Code",
          "x": 0.17057291666666666,
          "y": 0.13999999999999999,
          "uniqueID": "id-1008",
          "ioValues": [
            {
              "name": "frontCamberDeg",
              "ioValue": 61
            },
            {
              "name": "rearCamberDeg",
              "ioValue": 80
            }
          ],
          "interpreterVersion": 1,
          "code": "\n/**\n * This code atom implements a simple linear layout funtion which repeats\n * shape count times in the positive X direction with offset space\n * between each. Works for either 2D or 3D shapes. Negative offset\n * and negative counts invert the direction.\n * \n * Notice that defaults are set for both count and offset.\n */\n\nconst pointsAndSpline = (points: [number, number, number][]) => {\n  console.log('bah')\n  const result = [];\n  result.push(\n    replicad.makeBSplineApproximation(points, {\n      degMax: 3,\n      degMin: 1,\n      tolerance: 0.01,\n    })\n  );\n\n  const q = replicad.makeSphere(0.5);\n  for (const p of points) {\n    console.log(p);\n    result.push(q.clone().translate(p[0], p[1], p[2]));\n  }\n  return result;\n};\n\nfunction pointsWithCamberedStart(\n  points: [number, number][],\n  stations: number[],\n  frontCamber: number,\n  backCamber: number\n): [number, number, number][] {\n  const result = [];\n  for (let i = 0; i < points.length; i++) {\n    let x = stations[i];\n    let z = points[i][1];\n    if (i == 0) {\n      x = Math.cos(frontCamber) * z; // instead of 0\n      z = Math.sin(frontCamber) * z;\n    }\n    if (i == points.length - 1) {\n      x = x - Math.cos(backCamber) * z;\n      z = Math.sin(backCamber) * z;\n    }\n\n    result.push([x, points[i][0], z]);\n  }\n  //@ts-ignore\n  return result;\n};\n\nfunction run(frontCamberDeg: number, rearCamberDeg: number) {\n  const keel_line: [number, number][] = [\n    [0, 16 + 7 / 16],\n    [0, 18 + 7 / 8],\n    [0, 21 + 7 / 8],\n    [0, 21],\n    [0, 18 + 9 / 16],\n  ];\n\n  const stationDists = [23 + 1 / 8, 24, 24, 23 + 1 / 8];\n\n  const spoints = [0];\n  for (const offset of stationDists) {\n    spoints.push(spoints[spoints.length - 1] + offset);\n  }\n  const frontCamber = frontCamberDeg * Math.PI / 180\n  const rearCamber = rearCamberDeg * Math.PI / 180\n\n  const t = pointsWithCamberedStart(keel_line, spoints, frontCamber, rearCamber);\n  return t.map(p => replicad.makeVertex(p))\n\n\n\n  const blep = [\n    pointsAndSpline(pointsWithCamberedStart(keel_line, spoints, frontCamber, rearCamber)),\n    pointsAndSpline(pointsWithCamberedStart(chine, spoints, frontCamber, rearCamber)),\n    pointsAndSpline(pointsWithCamberedStart(sheer, spoints, frontCamber, rearCamber)),\n  ];\n\n  console.log(\"blep\")\n  console.log(blep)\n  console.log(blep.length)\n  const wires = []\n  for (const res of blep) {\n    //@ts-ignore\n    wires.push(replicad.assembleWire([res[0]]))\n  }\n  console.log(wires)\n  return replicad.loft(wires)\n}",
          "compiledCode": "\"use strict\";\n/**\n * This code atom implements a simple linear layout funtion which repeats\n * shape count times in the positive X direction with offset space\n * between each. Works for either 2D or 3D shapes. Negative offset\n * and negative counts invert the direction.\n *\n * Notice that defaults are set for both count and offset.\n */\nconst pointsAndSpline = (points) => {\n    console.log('bah');\n    const result = [];\n    result.push(replicad.makeBSplineApproximation(points, {\n        degMax: 3,\n        degMin: 1,\n        tolerance: 0.01,\n    }));\n    const q = replicad.makeSphere(0.5);\n    for (const p of points) {\n        console.log(p);\n        result.push(q.clone().translate(p[0], p[1], p[2]));\n    }\n    return result;\n};\nfunction pointsWithCamberedStart(points, stations, frontCamber, backCamber) {\n    const result = [];\n    for (let i = 0; i < points.length; i++) {\n        let x = stations[i];\n        let z = points[i][1];\n        if (i == 0) {\n            x = Math.cos(frontCamber) * z; // instead of 0\n            z = Math.sin(frontCamber) * z;\n        }\n        if (i == points.length - 1) {\n            x = x - Math.cos(backCamber) * z;\n            z = Math.sin(backCamber) * z;\n        }\n        result.push([x, points[i][0], z]);\n    }\n    //@ts-ignore\n    return result;\n}\n;\nfunction run(frontCamberDeg, rearCamberDeg) {\n    const keel_line = [\n        [0, 16 + 7 / 16],\n        [0, 18 + 7 / 8],\n        [0, 21 + 7 / 8],\n        [0, 21],\n        [0, 18 + 9 / 16],\n    ];\n    const stationDists = [23 + 1 / 8, 24, 24, 23 + 1 / 8];\n    const spoints = [0];\n    for (const offset of stationDists) {\n        spoints.push(spoints[spoints.length - 1] + offset);\n    }\n    const frontCamber = frontCamberDeg * Math.PI / 180;\n    const rearCamber = rearCamberDeg * Math.PI / 180;\n    const t = pointsWithCamberedStart(keel_line, spoints, frontCamber, rearCamber);\n    return t.map(p => replicad.makeVertex(p));\n    const blep = [\n        pointsAndSpline(pointsWithCamberedStart(keel_line, spoints, frontCamber, rearCamber)),\n        pointsAndSpline(pointsWithCamberedStart(chine, spoints, frontCamber, rearCamber)),\n        pointsAndSpline(pointsWithCamberedStart(sheer, spoints, frontCamber, rearCamber)),\n    ];\n    console.log(\"blep\");\n    console.log(blep);\n    console.log(blep.length);\n    const wires = [];\n    for (const res of blep) {\n        //@ts-ignore\n        wires.push(replicad.assembleWire([res[0]]));\n    }\n    console.log(wires);\n    return replicad.loft(wires);\n}\n"
        },
        {
          "atomType": "Code",
          "x": 0.34765625,
          "y": 0.47428571428571425,
          "uniqueID": "id-1012",
          "ioValues": [
            {
              "name": "points",
              "ioValue": "__GEOMETRY_INPUT__"
            }
          ],
          "interpreterVersion": 1,
          "code": "\n/**\n * This code atom implements a simple linear layout funtion which repeats\n * shape count times in the positive X direction with offset space\n * between each. Works for either 2D or 3D shapes. Negative offset\n * and negative counts invert the direction.\n * \n * Notice that defaults are set for both count and offset.\n */\n\nconst pointsAndSpline = (points: [number, number, number][]) => {\n  console.log('bah')\n  const result = [];\n  result.push(\n    replicad.makeBSplineApproximation(points, {\n      degMax: 3,\n      degMin: 1,\n      tolerance: 0.01,\n    })\n  );\n\n  const q = replicad.makeSphere(0.5);\n  for (const p of points) {\n    console.log(p);\n    result.push(q.clone().translate(p[0], p[1], p[2]));\n  }\n  return result;\n};\n\nfunction pointsWithCamberedStart(\n  points: [number, number][],\n  stations: number[],\n  frontCamber: number,\n  backCamber: number\n): [number, number, number][] {\n  const result = [];\n  for (let i = 0; i < points.length; i++) {\n    let x = stations[i];\n    let z = points[i][1];\n    if (i == 0) {\n      x = Math.cos(frontCamber) * z; // instead of 0\n      z = Math.sin(frontCamber) * z;\n    }\n    if (i == points.length - 1) {\n      x = x - Math.cos(backCamber) * z;\n      z = Math.sin(backCamber) * z;\n    }\n\n    result.push([x, points[i][0], z]);\n  }\n  //@ts-ignore\n  return result;\n};\n\nfunction run(points: Assembly) {\n  const p: [number,number,number][] = []\n  points.onLeafs((l) => {\n    if (l.isPoint()) {\n      p.push(l.geometry.asTuple())\n    }\n    else throw new Error(\"Requires an assembly of Points, received: \" + l.geometry.constructor.name)\n    return l;\n  })\n\n  return replicad.assembleWire([replicad.makeBSplineApproximation(p, {\n    degMax: 3,\n    degMin: 3,\n    tolerance: 0.0001,\n  })])\n\n\n  const keel_line: [number, number][] = [\n    [0, 16 + 7 / 16],\n    [0, 18 + 7 / 8],\n    [0, 21 + 7 / 8],\n    [0, 21],\n    [0, 18 + 9 / 16],\n  ];\n\n  const stationDists = [23 + 1 / 8, 24, 24, 23 + 1 / 8];\n\n  const spoints = [0];\n  for (const offset of stationDists) {\n    spoints.push(spoints[spoints.length - 1] + offset);\n  }\n  const frontCamber = frontCamberDeg * Math.PI / 180\n  const rearCamber = rearCamberDeg * Math.PI / 180\n\n  const t = pointsWithCamberedStart(keel_line, spoints, frontCamber, rearCamber);\n  return t.map(p => replicad.makeVertex(p))\n\n\n\n  const blep = [\n    pointsAndSpline(pointsWithCamberedStart(keel_line, spoints, frontCamber, rearCamber)),\n    pointsAndSpline(pointsWithCamberedStart(chine, spoints, frontCamber, rearCamber)),\n    pointsAndSpline(pointsWithCamberedStart(sheer, spoints, frontCamber, rearCamber)),\n  ];\n\n  console.log(\"blep\")\n  console.log(blep)\n  console.log(blep.length)\n  const wires = []\n  for (const res of blep) {\n    //@ts-ignore\n    wires.push(replicad.assembleWire([res[0]]))\n  }\n  console.log(wires)\n  return replicad.loft(wires)\n}",
          "compiledCode": "\"use strict\";\n/**\n * This code atom implements a simple linear layout funtion which repeats\n * shape count times in the positive X direction with offset space\n * between each. Works for either 2D or 3D shapes. Negative offset\n * and negative counts invert the direction.\n *\n * Notice that defaults are set for both count and offset.\n */\nconst pointsAndSpline = (points) => {\n    console.log('bah');\n    const result = [];\n    result.push(replicad.makeBSplineApproximation(points, {\n        degMax: 3,\n        degMin: 1,\n        tolerance: 0.01,\n    }));\n    const q = replicad.makeSphere(0.5);\n    for (const p of points) {\n        console.log(p);\n        result.push(q.clone().translate(p[0], p[1], p[2]));\n    }\n    return result;\n};\nfunction pointsWithCamberedStart(points, stations, frontCamber, backCamber) {\n    const result = [];\n    for (let i = 0; i < points.length; i++) {\n        let x = stations[i];\n        let z = points[i][1];\n        if (i == 0) {\n            x = Math.cos(frontCamber) * z; // instead of 0\n            z = Math.sin(frontCamber) * z;\n        }\n        if (i == points.length - 1) {\n            x = x - Math.cos(backCamber) * z;\n            z = Math.sin(backCamber) * z;\n        }\n        result.push([x, points[i][0], z]);\n    }\n    //@ts-ignore\n    return result;\n}\n;\nfunction run(points) {\n    const p = [];\n    points.onLeafs((l) => {\n        if (l.isPoint()) {\n            p.push(l.geometry.asTuple());\n        }\n        else\n            throw new Error(\"Requires an assembly of Points, received: \" + l.geometry.constructor.name);\n        return l;\n    });\n    return replicad.assembleWire([replicad.makeBSplineApproximation(p, {\n            degMax: 3,\n            degMin: 3,\n            tolerance: 0.0001,\n        })]);\n    const keel_line = [\n        [0, 16 + 7 / 16],\n        [0, 18 + 7 / 8],\n        [0, 21 + 7 / 8],\n        [0, 21],\n        [0, 18 + 9 / 16],\n    ];\n    const stationDists = [23 + 1 / 8, 24, 24, 23 + 1 / 8];\n    const spoints = [0];\n    for (const offset of stationDists) {\n        spoints.push(spoints[spoints.length - 1] + offset);\n    }\n    const frontCamber = frontCamberDeg * Math.PI / 180;\n    const rearCamber = rearCamberDeg * Math.PI / 180;\n    const t = pointsWithCamberedStart(keel_line, spoints, frontCamber, rearCamber);\n    return t.map(p => replicad.makeVertex(p));\n    const blep = [\n        pointsAndSpline(pointsWithCamberedStart(keel_line, spoints, frontCamber, rearCamber)),\n        pointsAndSpline(pointsWithCamberedStart(chine, spoints, frontCamber, rearCamber)),\n        pointsAndSpline(pointsWithCamberedStart(sheer, spoints, frontCamber, rearCamber)),\n    ];\n    console.log(\"blep\");\n    console.log(blep);\n    console.log(blep.length);\n    const wires = [];\n    for (const res of blep) {\n        //@ts-ignore\n        wires.push(replicad.assembleWire([res[0]]));\n    }\n    console.log(wires);\n    return replicad.loft(wires);\n}\n"
        },
        {
          "atomType": "Assembly",
          "x": 0.5188802083333334,
          "y": 0.41714285714285715,
          "uniqueID": "id-1026",
          "ioValues": [
            {
              "name": "Shape 1",
              "ioValue": null
            },
            {
              "name": "Shape 2",
              "ioValue": null
            }
          ]
        },
        {
          "atomType": "Input",
          "x": 0.025384615384615384,
          "y": 0.15384615384615385,
          "uniqueID": "id-1044",
          "type": "array"
        },
        {
          "atomType": "Code",
          "x": 0.255859375,
          "y": 0.8171428571428572,
          "uniqueID": "id-1064",
          "ioValues": [
            {
              "name": "interval",
              "ioValue": 24
            },
            {
              "name": "offsets",
              "ioValue": [
                "2",
                "4",
                "5",
                "6"
              ]
            }
          ],
          "interpreterVersion": 1,
          "code": "\n/**\n * This code atom implements a simple linear layout funtion which repeats\n * shape count times in the positive X direction with offset space\n * between each. Works for either 2D or 3D shapes. Negative offset\n * and negative counts invert the direction.\n * \n * Notice that defaults are set for both count and offset.\n */\nfunction run(interval: number, offsets: number[]) {\n  const points: [number, number, number][] = []\n  let x = 0;\n  offsets.forEach((offset) => {\n    points.push([x, offset, 0])\n    x += interval\n  })\n\n  return replicad.assembleWire([replicad.makeBSplineApproximation(points, {\n    degMax: 3,\n    degMin: 3,\n    tolerance: 0.0001,\n  })])\n\n}",
          "compiledCode": "\"use strict\";\n/**\n * This code atom implements a simple linear layout funtion which repeats\n * shape count times in the positive X direction with offset space\n * between each. Works for either 2D or 3D shapes. Negative offset\n * and negative counts invert the direction.\n *\n * Notice that defaults are set for both count and offset.\n */\nfunction run(interval, offsets) {\n    const points = [];\n    let x = 0;\n    offsets.forEach((offset) => {\n        points.push([x, offset, 0]);\n        x += interval;\n    });\n    return replicad.assembleWire([replicad.makeBSplineApproximation(points, {\n            degMax: 3,\n            degMin: 3,\n            tolerance: 0.0001,\n        })]);\n}\n"
        }
      ],
      "allConnectors": [
        {
          "ap2Name": "Shape 1",
          "ap1ID": "id-1008",
          "ap2ID": "id-1026"
        },
        {
          "ap2Name": "points",
          "ap1ID": "id-1008",
          "ap2ID": "id-1012"
        },
        {
          "ap2Name": "Shape 2",
          "ap1ID": "id-1012",
          "ap2ID": "id-1026"
        },
        {
          "ap2Name": "number or geometry",
          "ap1ID": "id-1026",
          "ap2ID": "id-1001"
        },
        {
          "ap2Name": "offsets",
          "ap1ID": "id-1044",
          "ap2ID": "id-1064"
        }
      ],
      "fileTypeVersion": 1
    },
    {
      "atomType": "Circle",
      "x": 0.291015625,
      "y": 0.66,
      "uniqueID": "id-1087",
      "ioValues": [
        {
          "name": "diameter",
          "ioValue": 10
        }
      ]
    },
    {
      "atomType": "Assembly",
      "x": 0.458984375,
      "y": 0.3342857142857143,
      "uniqueID": "id-1090",
      "ioValues": [
        {
          "name": "Shape 1",
          "ioValue": null
        }
      ]
    },
    {
      "atomType": "Extrude",
      "x": 0.4166666666666667,
      "y": 0.7714285714285715,
      "uniqueID": "id-1095",
      "ioValues": [
        {
          "name": "geometry",
          "ioValue": "__GEOMETRY_INPUT__"
        },
        {
          "name": "height",
          "ioValue": 10
        }
      ]
    },
    {
      "atomType": "Fusion",
      "x": 0.6067708333333334,
      "y": 0.3914285714285714,
      "uniqueID": "id-1099",
      "ioValues": [],
      "unionType": "Fusion"
    },
    {
      "atomType": "Code",
      "x": 0.25,
      "y": 0.18,
      "uniqueID": "id-1102",
      "ioValues": [
        {
          "name": "frontCamberDeg",
          "ioValue": 61
        },
        {
          "name": "rearCamberDeg",
          "ioValue": 80
        }
      ],
      "interpreterVersion": 1,
      "code": "\n/**\n * This code atom implements a simple linear layout funtion which repeats\n * shape count times in the positive X direction with offset space\n * between each. Works for either 2D or 3D shapes. Negative offset\n * and negative counts invert the direction.\n * \n * Notice that defaults are set for both count and offset.\n */\n\nconst pointsAndSpline = (points: [number, number, number][]) => {\n  console.log('bah')\n  const result = [];\n  result.push(\n    replicad.makeBSplineApproximation(points, {\n      degMax: 3,\n      degMin: 1,\n      tolerance: 0.01,\n    })\n  );\n\n  const q = replicad.makeSphere(0.5);\n  for (const p of points) {\n    console.log(p);\n    result.push(q.clone().translate(p[0], p[1], p[2]));\n  }\n  return result;\n};\n\nfunction pointsWithCamberedStart(\n  points: [number, number][],\n  stations: number[],\n  frontCamber: number,\n  backCamber: number\n): [number, number, number][] {\n  const result = [];\n  for (let i = 0; i < points.length; i++) {\n    let x = stations[i];\n    let z = points[i][1];\n    if (i == 0) {\n      x = Math.cos(frontCamber) * z; // instead of 0\n      z = Math.sin(frontCamber) * z;\n    }\n    if (i == points.length - 1) {\n      x = x - Math.cos(backCamber) * z;\n      z = Math.sin(backCamber) * z;\n    }\n\n    result.push([x, points[i][0], z]);\n  }\n  //@ts-ignore\n  return result;\n};\n\nfunction run(frontCamberDeg: number, rearCamberDeg: number) {\n  const keel_line: [number, number][] = [\n    [0, 16 + 7 / 16],\n    [0, 18 + 7 / 8],\n    [0, 21 + 7 / 8],\n    [0, 21],\n    [0, 18 + 9 / 16],\n  ];\n\n  const stationDists = [23 + 1 / 8, 24, 24, 23 + 1 / 8];\n\n  const spoints = [0];\n  for (const offset of stationDists) {\n    spoints.push(spoints[spoints.length - 1] + offset);\n  }\n  const frontCamber = frontCamberDeg * Math.PI / 180\n  const rearCamber = rearCamberDeg * Math.PI / 180\n\n  const t = pointsWithCamberedStart(keel_line, spoints, frontCamber, rearCamber);\n  return t.map(p => replicad.makeVertex(p))\n\n\n\n  const blep = [\n    pointsAndSpline(pointsWithCamberedStart(keel_line, spoints, frontCamber, rearCamber)),\n    pointsAndSpline(pointsWithCamberedStart(chine, spoints, frontCamber, rearCamber)),\n    pointsAndSpline(pointsWithCamberedStart(sheer, spoints, frontCamber, rearCamber)),\n  ];\n\n  console.log(\"blep\")\n  console.log(blep)\n  console.log(blep.length)\n  const wires = []\n  for (const res of blep) {\n    //@ts-ignore\n    wires.push(replicad.assembleWire([res[0]]))\n  }\n  console.log(wires)\n  return replicad.loft(wires)\n}",
      "compiledCode": "\"use strict\";\n/**\n * This code atom implements a simple linear layout funtion which repeats\n * shape count times in the positive X direction with offset space\n * between each. Works for either 2D or 3D shapes. Negative offset\n * and negative counts invert the direction.\n *\n * Notice that defaults are set for both count and offset.\n */\nconst pointsAndSpline = (points) => {\n    console.log('bah');\n    const result = [];\n    result.push(replicad.makeBSplineApproximation(points, {\n        degMax: 3,\n        degMin: 1,\n        tolerance: 0.01,\n    }));\n    const q = replicad.makeSphere(0.5);\n    for (const p of points) {\n        console.log(p);\n        result.push(q.clone().translate(p[0], p[1], p[2]));\n    }\n    return result;\n};\nfunction pointsWithCamberedStart(points, stations, frontCamber, backCamber) {\n    const result = [];\n    for (let i = 0; i < points.length; i++) {\n        let x = stations[i];\n        let z = points[i][1];\n        if (i == 0) {\n            x = Math.cos(frontCamber) * z; // instead of 0\n            z = Math.sin(frontCamber) * z;\n        }\n        if (i == points.length - 1) {\n            x = x - Math.cos(backCamber) * z;\n            z = Math.sin(backCamber) * z;\n        }\n        result.push([x, points[i][0], z]);\n    }\n    //@ts-ignore\n    return result;\n}\n;\nfunction run(frontCamberDeg, rearCamberDeg) {\n    const keel_line = [\n        [0, 16 + 7 / 16],\n        [0, 18 + 7 / 8],\n        [0, 21 + 7 / 8],\n        [0, 21],\n        [0, 18 + 9 / 16],\n    ];\n    const stationDists = [23 + 1 / 8, 24, 24, 23 + 1 / 8];\n    const spoints = [0];\n    for (const offset of stationDists) {\n        spoints.push(spoints[spoints.length - 1] + offset);\n    }\n    const frontCamber = frontCamberDeg * Math.PI / 180;\n    const rearCamber = rearCamberDeg * Math.PI / 180;\n    const t = pointsWithCamberedStart(keel_line, spoints, frontCamber, rearCamber);\n    return t.map(p => replicad.makeVertex(p));\n    const blep = [\n        pointsAndSpline(pointsWithCamberedStart(keel_line, spoints, frontCamber, rearCamber)),\n        pointsAndSpline(pointsWithCamberedStart(chine, spoints, frontCamber, rearCamber)),\n        pointsAndSpline(pointsWithCamberedStart(sheer, spoints, frontCamber, rearCamber)),\n    ];\n    console.log(\"blep\");\n    console.log(blep);\n    console.log(blep.length);\n    const wires = [];\n    for (const res of blep) {\n        //@ts-ignore\n        wires.push(replicad.assembleWire([res[0]]));\n    }\n    console.log(wires);\n    return replicad.loft(wires);\n}\n"
    },
    {
      "atomType": "Loft",
      "x": 0.6497395833333334,
      "y": 0.5942857142857143,
      "uniqueID": "id-1108",
      "ioValues": [
        {
          "name": "Shape 1",
          "ioValue": "__GEOMETRY_INPUT__"
        },
        {
          "name": "Shape 2",
          "ioValue": "__GEOMETRY_INPUT__"
        },
        {
          "name": "Shape 3",
          "ioValue": "__GEOMETRY_INPUT__"
        },
        {
          "name": "Shape 1",
          "ioValue": {
            "geometry": "move-circle-5-0-0-0",
            "dimension": "2D",
            "tags": [],
            "plane": {
              "origin": [
                0,
                0,
                10
              ],
              "xDir": [
                1,
                0,
                0
              ],
              "normal": [
                0,
                0,
                1
              ]
            },
            "color": "#aad7f2",
            "bom": []
          }
        },
        {
          "name": "Shape 2",
          "ioValue": {
            "geometry": "45494fd8-{\"geometry\":[{\"geometry\":\"e42d01a0-61-80-0\",\"color\":\"#3c5a6e\",\"tags\":[],\"bom\":[],\"plane\":{\"origin\":[0,0,0],\"xDir\":[1,0,0],\"normal\":[0,0,1]},\"dimension\":\"Point3D\"},{\"geometry\":\"e42d01a0-61-80-1\",\"color\":\"#3c5a6e\",\"tags\":[],\"bom\":[],\"plane\":{\"origin\":[0,0,0],\"xDir\":[1,0,0],\"normal\":[0,0,1]},\"dimension\":\"Point3D\"},{\"geometry\":\"e42d01a0-61-80-2\",\"color\":\"#3c5a6e\",\"tags\":[],\"bom\":[],\"plane\":{\"origin\":[0,0,0],\"xDir\":[1,0,0],\"normal\":[0,0,1]},\"dimension\":\"Point3D\"},{\"geometry\":\"e42d01a0-61-80-3\",\"color\":\"#3c5a6e\",\"tags\":[],\"bom\":[],\"plane\":{\"origin\":[0,0,0],\"xDir\":[1,0,0],\"normal\":[0,0,1]},\"dimension\":\"Point3D\"},{\"geometry\":\"e42d01a0-61-80-4\",\"color\":\"#3c5a6e\",\"tags\":[],\"bom\":[],\"plane\":{\"origin\":[0,0,0],\"xDir\":[1,0,0],\"normal\":[0,0,1]},\"dimension\":\"Point3D\"}],\"plane\":{\"origin\":[0,0,0],\"xDir\":[1,0,0],\"normal\":[0,0,1]},\"tags\":[],\"color\":\"#aad7f2\",\"bom\":[],\"dimension\":\"2D\",\"nonReplicadSerialized\":[]}-4-0",
            "color": "#3c5a6e",
            "tags": [],
            "bom": [],
            "plane": {
              "origin": [
                0,
                0,
                0
              ],
              "xDir": [
                1,
                0,
                0
              ],
              "normal": [
                0,
                0,
                1
              ]
            },
            "dimension": "Point3D"
          }
        },
        {
          "name": "Shape 3",
          "ioValue": null
        }
      ],
      "closedSelection": 0
    },
    {
      "atomType": "Move",
      "x": 0.5,
      "y": 0.5,
      "uniqueID": "id-1111",
      "ioValues": [
        {
          "name": "geometry",
          "ioValue": "__GEOMETRY_INPUT__"
        },
        {
          "name": "xDist",
          "ioValue": 0,
          "currentEquation": "0"
        },
        {
          "name": "yDist",
          "ioValue": 0,
          "currentEquation": "0"
        },
        {
          "name": "zDist",
          "ioValue": 10,
          "currentEquation": "10"
        }
      ]
    },
    {
      "atomType": "GitHubMolecule",
      "x": 0.5071614583333333,
      "y": 0.7599999999999999,
      "uniqueID": "id-1123",
      "name": "IthFromAssembly",
      "ioValues": [
        {
          "name": "geometry",
          "ioValue": "__GEOMETRY_INPUT__"
        },
        {
          "name": "i",
          "ioValue": 4,
          "currentEquation": "4"
        }
      ],
      "topLevel": false,
      "allAtoms": [
        {
          "atomType": "Output",
          "x": 0.98,
          "y": 0.5,
          "uniqueID": "id-1119",
          "ioValues": [
            {
              "name": "number or geometry",
              "ioValue": "__GEOMETRY_INPUT__"
            }
          ]
        },
        {
          "atomType": "Code",
          "x": 0.5026041666666666,
          "y": 0.6845238095238095,
          "uniqueID": "id-1120",
          "ioValues": [
            {
              "name": "geometry",
              "ioValue": "__GEOMETRY_INPUT__"
            },
            {
              "name": "i",
              "ioValue": 4
            }
          ],
          "interpreterVersion": 0,
          "code": "\n      const Inputs = [\n       {inputName: \"geometry\", type: \"geometry\", defaultValue: null},\n      {inputName: \"i\", type: \"number\", defaultValue: 0},\n      ]\n      //This defines the molecules inputs and creates variables with the same names which can be referenced in the code\n\n      //Gets the shape from the library - no need to clone or access geometry directly\n      let importedShape = library[geometry]\n      let j = 0;\n      console.log(\"foo\")\n      console.log(importedShape)\n      const leafList = await AssemblyAsIterable(importedShape)\n      console.log(leafList)\n      if (i >= leafList.length) {\n        throw new Error(`Assembly only has ${leafList.length} entries. Tried to find index: ${i}.`)\n      }\n      return leafList[i]\n\n\n      /**\n      Built-in Functions Available:\n      - Move(geometry, x, y, z) - Move a shape in 3D space\n      - Rotate(geometry, x, y, z) - Rotate a shape around X, Y, Z axes (degrees)\n      - Scale(geometry, factor) - Scale a shape by a factor\n      - Assembly([shapes]) - Combine multiple shapes into an assembly\n      - Intersect(shape1, shape2) - Boolean intersection of two shapes\n      - GetBounds(geometry) - Get the bounding box of a shape\n      - Fillet(geometry, radius) - Round edges with specified radius\n      - Chamfer(geometry, size) - Bevel edges with specified size\n\n      To Use the Code Atom, enter your inputs to the input list as an object array:\n      const Inputs = [\n        {inputName: \"shape\", type: \"geometry\", defaultValue: null},\n        {inputName: \"dist\", type: \"number\", defaultValue: 5},\n        {inputName: \"height\", type: \"number\", defaultValue: 10}\n      ]\n\n      Access imported geometry using library[inputName] - the built-in functions handle \n      the complexity of accessing geometry arrays and maintaining metadata automatically.\n\n      Simple Example - Move a shape:\n         const Inputs = [\n          {inputName: \"shape\", type: \"geometry\", defaultValue: null},\n          {inputName: \"x\", type: \"number\", defaultValue: 5}\n        ]\n        let movedShape = await Move(library[shape], x, 0, 0)\n        return movedShape\n      */\n      "
        },
        {
          "atomType": "Input",
          "x": 0.025384615384615384,
          "y": 0.15384615384615385,
          "uniqueID": "id-1121",
          "name": "geometry",
          "type": "geometry"
        },
        {
          "atomType": "Input",
          "x": 0.025384615384615384,
          "y": 0.24615384615384617,
          "uniqueID": "id-1122",
          "name": "i",
          "type": "number"
        }
      ],
      "allConnectors": [
        {
          "ap2Name": "number or geometry",
          "ap1ID": "id-1120",
          "ap2ID": "id-1119"
        },
        {
          "ap2Name": "geometry",
          "ap1ID": "id-1121",
          "ap2ID": "id-1120"
        },
        {
          "ap2Name": "i",
          "ap1ID": "id-1122",
          "ap2ID": "id-1120"
        }
      ],
      "parentRepo": {
        "lastFoundGit": "2026-05-08T18:23:39.228Z",
        "privateRepo": false,
        "repoName": "IthFromAssembly",
        "dateCreated": "2025-11-03T02:49:44Z",
        "likes": 0,
        "readMe": "https://raw.githubusercontent.com/tristan-huber/IthFromAssembly/master/README.md?sanitize=true",
        "yyyy": 2025,
        "topMoleculeID": "id-2718",
        "githubMoleculesUsed": [],
        "html_url": "https://github.com/tristan-huber/IthFromAssembly",
        "topics": [
          "abundance-tool"
        ],
        "parentRepo": null,
        "forks": 0,
        "dateModified": "739558",
        "ranking": 3.9,
        "searchField": "ithfromassembly tristan-huber get the ith leaf from an assembly abundance-tool",
        "owner": "tristan-huber",
        "description": "Get the ith leaf from an assembly",
        "contentURL": "https://raw.githubusercontent.com/tristan-huber/IthFromAssembly/master/project.abundance?sanitize=true",
        "svgURL": "https://raw.githubusercontent.com/tristan-huber/IthFromAssembly/master/project.svg?sanitize=true",
        "isLocal": false
      },
      "unitsKey": "Unitless",
      "fileTypeVersion": 1,
      "lastReloadedFromGithubAt": 1778277228196
    }
  ],
  "allConnectors": [
    {
      "ap2Name": "Shape 1",
      "ap1ID": "id-999",
      "ap2ID": "id-1090"
    },
    {
      "ap2Name": "geometry",
      "ap1ID": "id-1087",
      "ap2ID": "id-1095"
    },
    {
      "ap2Name": "geometry",
      "ap1ID": "id-1087",
      "ap2ID": "id-1111"
    },
    {
      "ap2Name": "geometry",
      "ap1ID": "id-1102",
      "ap2ID": "id-1123"
    },
    {
      "ap2Name": "Shape 1",
      "ap1ID": "id-1111",
      "ap2ID": "id-1108"
    },
    {
      "ap2Name": "Shape 2",
      "ap1ID": "id-1123",
      "ap2ID": "id-1108"
    }
  ],
  "unitsKey": "Inches",
  "fileTypeVersion": 1,
  "filetypeVersion": 1
}